title: Textseite
icon: page
extends: options/admin-only

columns:
  main:
    width: 2/3
    fields:
      text:
        extends: fields/richtext
        label: Text
  sidebar:
    width: 1/3
    fields:
      seo: fields/seo
