# Verwaltet von basis-schutz-os
# Anfragen an unbekannte Hostnamen / direkt an die IP werden verworfen.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444;
}
${DEFAULT_TLS_SERVER}
