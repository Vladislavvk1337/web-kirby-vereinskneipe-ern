# Staging (GRAV_ENVIRONMENT=staging): wie Produktion, aber nicht indexiert.
errors:
  display: 0
  log: true
twig:
  cache: true
  debug: false
  auto_reload: false
cache:
  check:
    method: file
session:
  secure: true
