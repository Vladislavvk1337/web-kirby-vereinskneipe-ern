# Nur Abweichungen vom Grav-Standard (Listen würden sonst ersetzt statt
# ergänzt). Der geheime Nonce-Schlüssel liegt nicht hier, sondern in
# $GRAV_DATA_DIR/config/security-private.php (setup.php, scripts/entrypoint.sh).

# Kein Twig in redaktionellen Inhalten – Templates liegen nur im Theme.
twig_content:
  process_enabled: false
  editor_enabled: false
  config_access: false
